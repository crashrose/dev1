<%= form_tag((add_stats_by_person_admin_competition_path(competition)), method: :get) do %>
  <%= select_tag "people", options_from_collection_for_select(Person.all, "id", "name"), include_blank: false %>
  <%= button_tag "Go!" %>
<% end %>
